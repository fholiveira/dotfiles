# vim: filetype=sh
#
if [ -z "$DOTFILES" ]; then
  export DOTFILES=~/.dotfiles
fi

chmod +x $DOTFILES/scripts/dotfiles

function dotfiles() {
  case $1 in
    browse)
      cd $DOTFILES
    ;;
    *)
      $DOTFILES/scripts/dotfiles "${@:1}" 
    ;;
  esac
}
