# vim: filetype=sh

export DOTSCRIPTS=$DOTFILES/scripts/dotfiles.d
source $DOTSCRIPTS/commands

case $1 in
  install)     install ;;
  update)      update ;;
  -h|--help|*) cat $DOTSCRIPTS/help ;;
esac
